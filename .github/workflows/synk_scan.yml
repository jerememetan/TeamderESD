name: Snyk scan for student-service
on:
  push:
    branches: [main]
jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: No student-service build (wrappers removed)
        run: |
          echo "student-service wrapper removed; skipping image build and Snyk scan"
